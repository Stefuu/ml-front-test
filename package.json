{
  "name": "root",
  "devDependencies": {
    "lerna": "^3.4.0"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts":{
    "app:start": "lerna run --stream --scope app start",
    "server:start": "lerna run --stream --scope server start" 
  }
}
